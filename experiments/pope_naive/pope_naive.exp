# configurations
BASE = --warmup_instructions=10000 --simulation_instructions=40000000
NOPREF = --config=$(PYTHIA_HOME)/config/nopref.ini
PYTHIA = --l2c_prefetcher_types=scooby --config=$(PYTHIA_HOME)/config/pythia.ini

nopref						$(BASE) $(NOPREF)
pythia						$(BASE) $(PYTHIA)
